[project]
name = "notion-tool-diff-mark"
version = "0.1.0"
description = "Package a Python local lab for diff analysis with framed sample traffic, bounds and ordering tests, and documented operating limits."
requires-python = ">=3.11"
